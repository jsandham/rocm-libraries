## Splice point 4a: .../kernel_ingestor_engine/IngestorPacks.hpp
## Add this declaration beside the other register<Name>Symbols() decls. Both this edit AND
## the IngestorPacks.cpp table-row edit (ingestor_packs.cpp.txt) are required: a pack
## registered here but missing from the table silently vanishes from the unit-test binary
## (the static-archive linker drops an unreferenced object) while surviving in the plugin
## .so.
/// @see packs/{{ config.native_class_name }}Native.cpp
void {{ config.register_symbols_fn }}(hipdnn_plugin_sdk::ingestor::SymbolScope<Handle>& scope);
{% if config.is_packaged %}
## Packaged dialect only: the emitted table row names this symbol as its resetModuleCache.
## Define it OUTSIDE the pack's anonymous namespace, or the row will not link.
void reset{{ config.engine.pascal_name }}ModuleCache();
{%- endif %}
